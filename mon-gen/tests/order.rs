extern crate mon_gen;

// Validate queue order and moves that effect the order.
#[test]
fn battle_order()
{
	// Assert move with low priority goes last.
	// Assert low priority goes first after.
	// Assert move that changes monster priority.
	// Assert monster priority works.
	// Assert switch still goes first.
	// Assert move that changes speed priority.
	// Assert switch still goes first.
	// Assert new speed taken into account.
}
