extern crate mon_gen;

// Validate multi-target attacks hit targets they claim.
#[test]
fn battle_target()
{
	// Assert move that targets single opponent.
	// Assert move that targets multiple opponents.
	// Assert move that targets self.
	// Assert move that targets allies.
	// Assert move that targets all.

	// Make sure that with all of these, target cancels out when dead.
	// But does not cancel out if target is multiple.
}
